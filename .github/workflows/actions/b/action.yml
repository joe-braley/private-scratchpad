
name: test

inputs:
  image:
    description: 'image'
    required: true
    type: string

runs:
  using: 'composite'
  steps:

    - name: Run test
      shell: bash
      run: |
        echo "called"
